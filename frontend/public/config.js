// Runtime configuration for the RepoSage frontend.
// Deployment platforms can replace this file (or inject a value before this
// script runs) to point the app at the production backend. When unset, the app
// talks to the same origin that served the page. Never hardcode a dev URL here.
window.__RUNTIME_API_URL__ = window.__RUNTIME_API_URL__ || "";
